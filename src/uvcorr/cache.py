"""Sibling HDF5 UV cache (``<name>.dat.uv.h5``) built from a raw ``.dat`` file.

Planned API (plan sections 6.1 and 7): ``UVCache`` with ``build_from_dat``,
``is_valid_for``, ``boards``, ``load_board``, ``channel_data``, ``channels`` and
the result/override persistence methods. Implemented in phase 1.
"""
