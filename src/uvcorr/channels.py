"""Active-channel definitions, electrode labels and polarity.

Planned API (plan section 7): ``is_active_channel(rena, ch)``, ``ACTIVE_CHANNELS``,
``electrode_label()``, ``is_cathode()``, wrapping adc2kev's ``ElectrodeMap`` and
``tools.geometry``. Implemented in phase 1.
"""
