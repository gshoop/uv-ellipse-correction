"""Per-channel, per-board and whole-system analysis driver.

Planned API (plan section 7): ``FitOptions``, a channel key, ``ChannelResult``,
``analyze_channel``, ``analyze_board`` and ``analyze_all`` (process pool, one
task per board). Implemented in phase 3.
"""
